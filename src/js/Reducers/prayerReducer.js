import * as types from '../Actions/actionTypes';

export function prayerReducer(state = {
}, action) {
    switch (action.type) {
        default:
            return state;
            break;
    }
}

export default prayerReducer;