import * as types from '../Actions/actionTypes';

export function newReducer(state = {
}, action) {
    switch (action.type) {
        default:
            return state;
            break;
    }
}

export default newReducer;